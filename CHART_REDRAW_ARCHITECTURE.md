# 🎯 CHART REDRAW ARCHITECTURE — Архитектура перерисовки графика

## 📋 Содержание

1. [Смена таймфрейма — Полная перерисовка](#смена-таймфрейма)
2. [Смена режима свечей — Частичная перерисовка](#смена-режима-свечей)
3. [Смена типа графика — Полная перерисовка](#смена-типа-графика)
4. [Сравнительная таблица](#сравнительная-таблица)
5. [Детали реализации](#детали-реализации)

---

## Смена таймфрейма — Полная перерисовка

### Определение

**Полная перерисовка** означает полный сброс всех компонентов графика и загрузку новых данных для нового таймфрейма.

### Что происходит при смене таймфрейма

Когда пользователь меняет таймфрейм (например, с `5s` на `30s`), происходит следующее:

#### 1. Обнаружение изменения

```typescript
// useChart.ts
const previousTimeframeRef = useRef<string>(timeframe);

useEffect(() => {
  const timeframeChanged = previousTimeframeRef.current !== timeframe;
  if (timeframeChanged) {
    previousTimeframeRef.current = timeframe;
    // ... полный reset
  }
}, [timeframe]);
```

#### 2. Полный reset всех компонентов

**Шаг 1: Reset Viewport**
```typescript
viewport.reset();
```
**Что сбрасывается:**
- `viewportRef.current = null` — viewport полностью очищается
- `followModeRef.current = true` — включается follow mode
- `targetViewportRef.current = null` — очищается анимация follow
- `followAnimationStartRef.current = null` — очищается старт анимации
- `yDragRef.current = null` — очищается Y-scale drag
- `latestCandleTimeRef.current = null` — очищается якорь времени
- `configRef.current` — пересчитывается `visibleCandles` на основе нового таймфрейма

**Результат:** Viewport полностью пересчитывается заново при следующем `recalculateViewport()`.

---

**Шаг 2: Reset Interactions**
```typescript
chartInteractions.reset();
```
**Что сбрасывается:**
- `interactionStateRef.current = { isDragging: false, lastX: null }` — прерывается активный pan
- `yDragStateRef.current = false` — прерывается Y-scale drag
- `panInertiaRefs.activeRef.current = false` — останавливается инерция
- `panInertiaRefs.velocityRef.current = 0` — сбрасывается скорость

**Результат:** Все взаимодействия пользователя прерываются.

---

**Шаг 3: Reset данных свечей**
```typescript
chartData.reset();
```
**Что сбрасывается:**
```typescript
// useChartData.ts
const reset = (): void => {
  candlesRef.current = [];        // Очищаются ВСЕ исторические свечи
  liveCandleRef.current = null;    // Очищается live-свеча
};
```

**Результат:** Все данные свечей удаляются из памяти. График становится пустым.

---

**Шаг 4: Reset анимации**
```typescript
candleAnimator.reset();
```
**Что сбрасывается:**
```typescript
// useCandleAnimator.ts
const reset = (): void => {
  animationStateRef.current = null;  // Очищается состояние анимации
  lastUpdateTimeRef.current = 0;    // Сбрасывается время последнего обновления
};
```

**Результат:** Анимация live-свечи полностью останавливается и очищается.

---

**Шаг 5: Reset History Loader**
```typescript
historyLoader.reset();
```
**Что сбрасывается:**
```typescript
// useHistoryLoader.ts
const reset = (): void => {
  isLoadingRef.current = false;     // Сбрасывается флаг загрузки
  lastLoadTimeRef.current = null;   // Очищается время последней загрузки
  // Очищаются все внутренние состояния загрузчика истории
};
```

**Результат:** Загрузчик истории готов к загрузке новых данных для нового таймфрейма.

---

**Шаг 6: Reset Countdown Timer**
```typescript
candleCountdown.reset();
```
**Что сбрасывается:**
```typescript
// useCandleCountdown.ts
const reset = (): void => {
  // Сбрасывается состояние таймера обратного отсчета
  // Таймер будет пересчитан при следующем обновлении
};
```

**Результат:** Таймер обратного отсчета сбрасывается.

---

#### 3. Загрузка новых данных

После полного reset происходит загрузка snapshot для нового таймфрейма:

```typescript
// useChart.ts
const snapshot = await fetch(`/api/terminal/snapshot?instrument=${instrument}&timeframe=${timeframe}`);

// Инициализация из нового snapshot
chartData.initializeFromSnapshot(
  snapshot.candles.items,
  snapshot.price.value,
  snapshot.price.timestamp,
  newTimeframeMs  // НОВЫЙ таймфрейм
);
```

**Что происходит:**
- Загружаются свечи для нового таймфрейма (например, 30-секундные вместо 5-секундных)
- Инициализируется новая live-свеча
- Viewport пересчитывается на основе новых данных
- График полностью перерисовывается с нуля

---

### Почему полная перерисовка?

**Причины:**

1. **Разные данные:**
   - 5s таймфрейм: свечи закрываются каждые 5 секунд
   - 30s таймфрейм: свечи закрываются каждые 30 секунд
   - Это **разные агрегации** одних и тех же тиков

2. **Разные временные слоты:**
   - Свечи для разных таймфреймов имеют разные `startTime` и `endTime`
   - Нельзя просто "пересчитать" существующие свечи

3. **Разный viewport:**
   - `visibleCandles` пересчитывается на основе нового таймфрейма
   - Viewport автоматически "отодвигается назад" для больших таймфреймов

4. **Разные WebSocket события:**
   - `candle:close` приходит с разной частотой
   - `candle:update` работает по-разному для разных таймфреймов

---

## Смена режима свечей — Частичная перерисовка

### Определение

**Частичная перерисовка** означает изменение только способа отображения данных без изменения самих данных или состояния графика.

### Что происходит при смене режима свечей

Когда пользователь меняет режим отображения (например, с `classic` на `heikin_ashi`), происходит следующее:

#### 1. Обнаружение изменения

```typescript
// useCandleMode.ts
const modeRef = useRef<CandleMode>('classic');

const setMode = (mode: CandleMode): void => {
  modeRef.current = mode;  // Просто меняется ref, НЕТ reset!
};
```

**Важно:** Нет никакого reset, только изменение ref.

---

#### 2. Что НЕ меняется

✅ **Данные остаются теми же:**
- `candlesRef.current` — массив свечей не меняется
- `liveCandleRef.current` — live-свеча не меняется
- Исторические данные остаются в памяти

✅ **Viewport остается тем же:**
- `viewportRef.current` — viewport не сбрасывается
- Zoom/pan состояние сохраняется
- Follow mode состояние сохраняется

✅ **Состояние взаимодействий сохраняется:**
- Pan/zoom состояние не сбрасывается
- Y-scale drag состояние сохраняется
- Inertia состояние сохраняется

✅ **Анимация продолжается:**
- `candleAnimator` продолжает работать
- Live-свеча продолжает обновляться

✅ **История сохраняется:**
- `historyLoader` не сбрасывается
- Загруженная история остается в памяти

---

#### 3. Что меняется

**Только способ трансформации данных для рендера:**

```typescript
// useCandleMode.ts
const getRenderCandles = (): Candle[] => {
  const candles = getCandles();  // Берем те же данные
  const mode = modeRef.current;

  switch (mode) {
    case 'classic':
      return candles;  // Возвращаем как есть

    case 'heikin_ashi':
      return transformToHeikinAshi(candles);  // Трансформируем

    case 'bars':
      return transformToBars(candles);  // Возвращаем как есть (трансформация в рендере)
  }
};
```

**Трансформация происходит "на лету":**
- Исходные данные не мутируются
- Создается новый массив трансформированных свечей
- Трансформация происходит каждый кадр при рендере

---

#### 4. Трансформации по режимам

**Classic (классические свечи):**
```typescript
// Данные не меняются
return candles;
```
- Используются исходные OHLC значения
- Отрисовка: тело свечи (open-close) + фитили (high-low)

---

**Heikin Ashi:**
```typescript
// heikinAshi.ts
export function transformToHeikinAshi(candles: Candle[]): Candle[] {
  // Формулы:
  // HA_close = (O + H + L + C) / 4
  // HA_open = (prev_HA_open + prev_HA_close) / 2
  // HA_high = max(H, HA_open, HA_close)
  // HA_low = min(L, HA_open, HA_close)
  
  // Создаются НОВЫЕ свечи (не мутируются исходные!)
  return haCandles;
}
```
- Данные трансформируются по формулам Heikin Ashi
- Отрисовка: тот же способ, что и classic (тело + фитили)
- Трансформация происходит каждый кадр

---

**Bars (OHLC бары):**
```typescript
// barTransform.ts
export function transformToBars(candles: Candle[]): Candle[] {
  // Данные не меняются, только способ отрисовки
  return candles;
}
```
- Данные не меняются
- Отрисовка: вертикальная линия (high-low) + горизонтальные черточки (open слева, close справа)

---

#### 5. Рендер использует трансформированные данные

```typescript
// useRenderLoop.ts
const render = (now: number) => {
  // ...
  const renderCandles = candleMode.getRenderCandles();  // Получаем трансформированные свечи
  const renderLiveCandle = candleMode.getRenderLiveCandle();  // Получаем трансформированную live-свечу
  
  // Отрисовка использует трансформированные данные
  renderCandles({
    ctx,
    viewport,
    candles: renderCandles,  // Трансформированные свечи
    liveCandle: renderLiveCandle,  // Трансформированная live-свеча
    mode: candleMode.getMode(),  // Режим для выбора способа отрисовки
    // ...
  });
};
```

**Важно:** Трансформация происходит каждый кадр, но исходные данные остаются неизменными.

---

### Почему частичная перерисовка?

**Причины:**

1. **Те же данные:**
   - Все режимы работают с одними и теми же исходными свечами
   - Разница только в способе отображения

2. **Трансформация "на лету":**
   - Heikin Ashi вычисляется каждый кадр из исходных данных
   - Не нужно сохранять трансформированные данные

3. **Viewport не зависит от режима:**
   - Viewport работает с временными диапазонами
   - Режим свечей не влияет на временные диапазоны

4. **Плавный переход:**
   - Пользователь может переключаться между режимами без потери контекста
   - Zoom/pan состояние сохраняется

---

## Сравнительная таблица

| Компонент | Смена таймфрейма | Смена режима свечей |
|-----------|------------------|---------------------|
| **Данные свечей** | ❌ Полностью очищаются | ✅ Остаются теми же |
| **Live-свеча** | ❌ Очищается | ✅ Остается той же |
| **Viewport** | ❌ Полностью сбрасывается | ✅ Сохраняется |
| **Zoom/Pan** | ❌ Сбрасывается | ✅ Сохраняется |
| **Follow mode** | ✅ Включается (reset) | ✅ Сохраняется |
| **Y-scale drag** | ❌ Сбрасывается | ✅ Сохраняется |
| **Inertia** | ❌ Останавливается | ✅ Сохраняется |
| **Анимация** | ❌ Сбрасывается | ✅ Продолжается |
| **История** | ❌ Очищается | ✅ Сохраняется |
| **Countdown timer** | ❌ Сбрасывается | ✅ Продолжается |
| **WebSocket подписка** | ✅ Обновляется (новый таймфрейм) | ✅ Не меняется |
| **Snapshot** | ✅ Загружается новый | ❌ Не загружается |
| **Трансформация данных** | ❌ Не применяется | ✅ Применяется (Heikin Ashi) |
| **Способ отрисовки** | ✅ Классический (всегда) | ✅ Меняется (classic/HA/bars) |
| **Смена типа графика** | ❌ Полная перерисовка | ❌ Не применимо (только для candles) |

---

## Детали реализации

### 1. Смена таймфрейма — Полный reset

**Файл:** `frontend/components/chart/useChart.ts`

```typescript
// 🔥 FLOW: Timeframe Switch Reset - полный reset при смене timeframe
const previousTimeframeRef = useRef<string>(timeframe);

useEffect(() => {
  const timeframeChanged = previousTimeframeRef.current !== timeframe;
  if (timeframeChanged) {
    previousTimeframeRef.current = timeframe;
    
    // 🔥 FLOW: Полный reset при смене timeframe
    // Шаг 1: Reset viewport (сброс zoom/pan, follow mode = true)
    viewport.reset();
    
    // Шаг 2: Reset interactions (сброс pan/zoom состояния)
    chartInteractions.reset();
    
    // Шаг 3: Reset данных свечей
    chartData.reset();
    
    // Шаг 4: Reset анимации
    candleAnimator.reset();
    
    // Шаг 5: Reset history loader
    historyLoader.reset();
    
    // Шаг 6: Reset countdown timer
    candleCountdown.reset();
  }
  
  // После reset загружается новый snapshot
  if (snapshot) {
    chartData.initializeFromSnapshot(
      snapshot.candles.items,
      snapshot.price.value,
      snapshot.price.timestamp,
      parseTimeframeToMs(timeframe)  // НОВЫЙ таймфрейм
    );
  }
}, [timeframe, snapshot]);
```

**Последовательность:**

1. **Обнаружение изменения** → `timeframeChanged = true`
2. **Reset всех компонентов** → полная очистка состояния
3. **Загрузка snapshot** → новые данные для нового таймфрейма
4. **Пересчет viewport** → автоматически при следующем `recalculateViewport()`
5. **Перерисовка** → график полностью перерисовывается с новыми данными

---

### 2. Смена режима свечей — Только изменение ref

**Файл:** `frontend/components/chart/internal/candleModes/useCandleMode.ts`

```typescript
export function useCandleMode({
  getCandles,
  getLiveCandle,
}: UseCandleModeParams): UseCandleModeReturn {
  // Хранение режима через useRef (не useState!)
  const modeRef = useRef<CandleMode>('classic');

  const setMode = (mode: CandleMode): void => {
    modeRef.current = mode;  // Только изменение ref, НЕТ reset!
  };

  const getRenderCandles = (): Candle[] => {
    const candles = getCandles();  // Берем те же данные
    const mode = modeRef.current;

    switch (mode) {
      case 'classic':
        return candles;  // Как есть

      case 'heikin_ashi':
        return transformToHeikinAshi(candles);  // Трансформация

      case 'bars':
        return transformToBars(candles);  // Как есть (трансформация в рендере)
    }
  };

  const getRenderLiveCandle = (): Candle | null => {
    const liveCandle = getLiveCandle();  // Берем ту же live-свечу
    if (!liveCandle) return null;

    const mode = modeRef.current;

    switch (mode) {
      case 'classic':
        return liveCandle;  // Как есть

      case 'heikin_ashi': {
        // Трансформируем live-свечу в Heikin Ashi
        const candles = getCandles();
        const haCandles = transformToHeikinAshi(candles);
        const prevHaCandle = haCandles[haCandles.length - 1];
        return transformLiveCandleToHeikinAshi(liveCandle, prevHaCandle);
      }

      case 'bars':
        return liveCandle;  // Как есть (трансформация в рендере)
    }
  };

  return {
    getRenderCandles,
    getRenderLiveCandle,
    setMode,
    getMode,
  };
}
```

**Последовательность:**

1. **Изменение режима** → `modeRef.current = newMode`
2. **Следующий кадр рендера** → `getRenderCandles()` возвращает трансформированные данные
3. **Отрисовка** → используется новый способ отрисовки (если bars)
4. **Данные остаются** → исходные свечи не меняются

---

### 3. Трансформация Heikin Ashi

**Файл:** `frontend/components/chart/internal/candleModes/heikinAshi.ts`

```typescript
export function transformToHeikinAshi(candles: Candle[]): Candle[] {
  if (candles.length === 0) return [];

  const haCandles: Candle[] = [];
  let prevHaOpen: number | null = null;
  let prevHaClose: number | null = null;

  for (let i = 0; i < candles.length; i++) {
    const candle = candles[i];
    
    // HA_close = (O + H + L + C) / 4
    const haClose = (candle.open + candle.high + candle.low + candle.close) / 4;

    // HA_open
    let haOpen: number;
    if (i === 0) {
      // Первая свеча: HA_open = (O + C) / 2
      haOpen = (candle.open + candle.close) / 2;
    } else {
      // Остальные: HA_open = (prev_HA_open + prev_HA_close) / 2
      haOpen = ((prevHaOpen ?? 0) + (prevHaClose ?? 0)) / 2;
    }

    // HA_high = max(H, HA_open, HA_close)
    const haHigh = Math.max(candle.high, haOpen, haClose);

    // HA_low = min(L, HA_open, HA_close)
    const haLow = Math.min(candle.low, haOpen, haClose);

    // Создаём НОВУЮ свечу (не мутируем исходную!)
    haCandles.push({
      open: haOpen,
      high: haHigh,
      low: haLow,
      close: haClose,
      startTime: candle.startTime,  // Время не меняется
      endTime: candle.endTime,      // Время не меняется
      isClosed: candle.isClosed,
    });

    // Сохраняем для следующей итерации
    prevHaOpen = haOpen;
    prevHaClose = haClose;
  }

  return haCandles;  // Новый массив, исходный не изменен
}
```

**Важно:**
- Исходные свечи **не мутируются**
- Создается **новый массив** трансформированных свечей
- Трансформация происходит **каждый кадр** при рендере
- Время (`startTime`, `endTime`) не меняется

---

### 4. Отрисовка по режимам

**Файл:** `frontend/components/chart/internal/render/renderCandles.ts`

```typescript
function renderCandle(
  ctx: CanvasRenderingContext2D,
  candle: Candle,
  viewport: Viewport,
  width: number,
  height: number,
  candleWidth: number,
  timeframeMs: number,
  isLive: boolean,
  mode: CandleMode
): void {
  if (mode === 'bars') {
    // Bars: вертикальная линия + горизонтальные черточки
    renderCandleBars(ctx, candle, viewport, width, height, candleWidth, timeframeMs);
  } else {
    // Classic или Heikin Ashi: тело свечи + фитили
    renderCandleClassic(ctx, candle, viewport, width, height, candleWidth, timeframeMs);
  }
}
```

**Classic / Heikin Ashi:**
- Тело свечи (прямоугольник от open до close)
- Фитили (линии от high до low)
- Цвет: зеленый если close >= open, красный если close < open

**Bars:**
- Вертикальная линия от low до high
- Горизонтальная черточка слева = open
- Горизонтальная черточка справа = close
- Цвет: зеленый если close >= open, красный если close < open

---

## Архитектурные принципы

### 1. Разделение ответственности

**Данные (useChartData):**
- Хранит исходные свечи
- Не знает о режимах отображения
- Не трансформирует данные

**Трансформация (useCandleMode):**
- Трансформирует данные "на лету"
- Не сохраняет трансформированные данные
- Не мутирует исходные данные

**Отрисовка (renderCandles):**
- Использует трансформированные данные
- Выбирает способ отрисовки по режиму
- Не знает о трансформации данных

---

### 2. Иммутабельность данных

**Принцип:**
- Исходные данные никогда не мутируются
- Трансформация создает новые объекты
- Каждый кадр рендера получает свежие трансформированные данные

**Пример:**
```typescript
// ❌ ПЛОХО (мутация)
candle.close = (candle.open + candle.high + candle.low + candle.close) / 4;

// ✅ ХОРОШО (иммутабельность)
const haCandle = {
  ...candle,
  close: (candle.open + candle.high + candle.low + candle.close) / 4,
};
```

---

### 3. Производительность

**Смена таймфрейма:**
- Полный reset необходим, так как данные полностью другие
- Загрузка snapshot происходит один раз
- Перерисовка происходит естественным образом при следующем кадре

**Смена режима свечей:**
- Трансформация происходит каждый кадр (не сохраняется)
- Это нормально, так как трансформация быстрая (O(n))
- Для Heikin Ashi: ~100-300 свечей → трансформация < 1ms
- Для больших графиков можно добавить кэширование (но пока не нужно)

---

## Итоговая схема

### Смена таймфрейма

```
Пользователь меняет таймфрейм (5s → 30s)
         ↓
Обнаружение изменения (useEffect)
         ↓
┌─────────────────────────────────────┐
│  ПОЛНЫЙ RESET ВСЕХ КОМПОНЕНТОВ      │
├─────────────────────────────────────┤
│ 1. viewport.reset()                 │
│    → viewport = null                 │
│    → follow mode = true              │
│    → zoom/pan = сброшен             │
│                                      │
│ 2. chartInteractions.reset()        │
│    → pan/zoom состояние = сброшено  │
│    → inertia = остановлена          │
│                                      │
│ 3. chartData.reset()                │
│    → candles = []                    │
│    → liveCandle = null               │
│                                      │
│ 4. candleAnimator.reset()           │
│    → анимация = сброшена             │
│                                      │
│ 5. historyLoader.reset()            │
│    → история = очищена                │
│                                      │
│ 6. candleCountdown.reset()          │
│    → таймер = сброшен                │
└─────────────────────────────────────┘
         ↓
Загрузка нового snapshot
         ↓
Инициализация из snapshot
         ↓
Пересчет viewport
         ↓
Полная перерисовка графика
```

### Смена режима свечей

```
Пользователь меняет режим (classic → heikin_ashi)
         ↓
setMode('heikin_ashi')
         ↓
modeRef.current = 'heikin_ashi'
         ↓
Следующий кадр рендера
         ↓
getRenderCandles() вызывается
         ↓
┌─────────────────────────────────────┐
│  ТРАНСФОРМАЦИЯ ДАННЫХ "НА ЛЕТУ"     │
├─────────────────────────────────────┤
│ const candles = getCandles()        │
│   → исходные свечи (не меняются)    │
│                                      │
│ const mode = modeRef.current         │
│   → 'heikin_ashi'                   │
│                                      │
│ return transformToHeikinAshi(candles)│
│   → новый массив HA свечей           │
│   → исходные свечи не мутированы     │
└─────────────────────────────────────┘
         ↓
renderCandles() использует трансформированные данные
         ↓
Отрисовка с новым способом отображения
         ↓
График перерисовывается (данные те же, вид другой)
```

---

## Смена типа графика — Полная перерисовка

### Определение

**Смена типа графика** означает переключение между свечным графиком (`candles`) и линейным графиком (`line`). Это также требует полной перерисовки, так как меняется модель данных.

### Что происходит при смене типа графика

Когда пользователь меняет тип графика (например, с `candles` на `line`), происходит следующее:

#### 1. Пересоздание компонента через React key

```typescript
// terminal/page.tsx
<ChartContainer
  key={`${instrument}_${timeframe}_${chartType}`}  // chartType включен в key
  type={chartType}
  // ...
/>
```

**Важно:** `chartType` включен в `key`, что заставляет React полностью пересоздать компонент при смене типа графика.

---

#### 2. Reset при монтировании компонента

**Для свечного графика (`useChart.ts`):**

```typescript
// 🔥 FLOW C-CHART-TYPE-RESET: Reset при монтировании компонента
const isInitialMountRef = useRef<boolean>(true);

useEffect(() => {
  if (isInitialMountRef.current) {
    isInitialMountRef.current = false;
    
    // Полный reset при первом монтировании (или при пересоздании из-за смены chartType)
    // Шаг 1: Reset viewport (сброс zoom/pan, follow mode = true)
    viewport.reset();
    
    // Шаг 2: Reset interactions (сброс pan/zoom состояния, инерции)
    chartInteractions.reset();
    
    // Шаг 3: Reset данных свечей
    chartData.reset();
    
    // Шаг 4: Reset анимации
    candleAnimator.reset();
    
    // Шаг 5: Reset history loader
    historyLoader.reset();
    
    // Шаг 6: Reset countdown timer
    candleCountdown.reset();
    
    // Шаг 7: Гарантируем follow mode и остановку инерции
    viewport.setFollowMode(true);
    chartInteractions.stopInertia?.();
  }
}, []); // Пустой массив зависимостей - только при монтировании
```

**Для линейного графика (`useLineChart.ts`):**

```typescript
// 🔥 FLOW C-CHART-TYPE-RESET: Reset при монтировании компонента
const isInitialMountRef = useRef<boolean>(true);

useEffect(() => {
  if (isInitialMountRef.current) {
    isInitialMountRef.current = false;
    
    // Полный reset при первом монтировании (или при пересоздании из-за смены chartType)
    // Шаг 1: Reset данных точек
    pointStore.reset();
    setLiveSegment(null);
    
    // Шаг 2: Reset viewport (сброс zoom/pan, autoFollow = true)
    const now = Date.now();
    viewport.setViewport(now - 60000, now, true); // Дефолтное окно 60 секунд
    
    // Шаг 3: Гарантируем autoFollow
    viewport.setAutoFollow(true);
    
    // Шаг 4: Останавливаем инерцию (если была активна)
    if (panInertiaRefsRef.current) {
      panInertiaRefsRef.current.activeRef.current = false;
      panInertiaRefsRef.current.velocityRef.current = 0;
    }
  }
}, []); // Пустой массив зависимостей - только при монтировании
```

---

#### 3. Что сбрасывается

**Свечной график:**
- ✅ Viewport (zoom/pan, follow mode)
- ✅ Interactions (pan/zoom состояние, инерция)
- ✅ Данные свечей
- ✅ Анимация live-свечи
- ✅ History loader
- ✅ Countdown timer

**Линейный график:**
- ✅ Данные точек (pointStore)
- ✅ Live сегмент
- ✅ Viewport (zoom/pan, autoFollow)
- ✅ Inertia (если была активна)

---

#### 4. Почему полная перерисовка?

**Причины:**

1. **Разные модели данных:**
   - Свечной график: работает со свечами (OHLC)
   - Линейный график: работает с точками (price points)
   - Это **разные структуры данных**

2. **Разные viewport контроллеры:**
   - Свечной: `useViewport` (работает со свечами)
   - Линейный: `useLineViewport` (работает с временными окнами)
   - Разные API и логика

3. **Разные render loops:**
   - Свечной: `useRenderLoop` (рендерит свечи)
   - Линейный: свой render loop (рендерит линию)
   - Разные способы отрисовки

4. **Разные WebSocket события:**
   - Свечной: подписан на `candle:update`, `candle:close`
   - Линейный: подписан на `price:update`
   - Разные источники данных

---

### Инварианты

**Важно понимать:**

1. **chartType ≠ candleMode:**
   - `chartType` меняет модель данных (candles ↔ line)
   - `candleMode` меняет только способ отображения (classic ↔ HA ↔ bars)
   - Это разные уровни абстракции

2. **Reset при монтировании:**
   - Reset происходит при первом монтировании компонента
   - При смене chartType компонент полностью пересоздается (благодаря key)
   - Поэтому reset при монтировании автоматически срабатывает при смене типа

3. **Не наследование состояния:**
   - Свечной график не наследует состояние от линейного
   - Линейный график не наследует состояние от свечного
   - Каждый тип графика имеет свое чистое состояние

---

## 📝 Примечания

### Почему при смене таймфрейма полная перерисовка?

1. **Разные агрегации данных:**
   - 5s свечи и 30s свечи — это разные агрегации тиков
   - Нельзя просто "пересчитать" существующие свечи

2. **Разные временные слоты:**
   - Свечи для разных таймфреймов имеют разные временные границы
   - Viewport должен пересчитаться для нового таймфрейма

3. **Разные WebSocket события:**
   - `candle:close` приходит с разной частотой
   - Нужна новая подписка на события

### Почему при смене режима частичная перерисовка?

1. **Те же данные:**
   - Все режимы работают с одними и теми же исходными свечами
   - Разница только в способе отображения

2. **Трансформация "на лету":**
   - Heikin Ashi вычисляется каждый кадр из исходных данных
   - Не нужно сохранять трансформированные данные

3. **Плавный UX:**
   - Пользователь может переключаться между режимами без потери контекста
   - Zoom/pan состояние сохраняется

---

**Версия:** 1.0  
**Дата:** 2026-01-30  
**Автор:** Chart Redraw Architecture Documentation
